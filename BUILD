To generate "make.conf" and build use:

    ./configure
    make

The configure system is written in javascript, and thus the minimal version of Jsi (jsimin) is built first.

For various predefined configurations, See Configs/ and use ./configure --config=XXX

